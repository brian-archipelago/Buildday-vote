# Required: OpenAI API key. Used for both Realtime transcription
# (gpt-4o-transcribe) and the pitch boundary analyzer (gpt-5.4-mini).
OPENAI_API_KEY=sk-...

# Required: Admin access token. Anyone with this can control the poll.
ADMIN_TOKEN=change-me-before-the-event
